# 密钥处置说明

> 处置日期：2026-09-21
> 适用范围：`patent/patent`（后端仓库）、`Patent-Management-Platform-修改1`（前端）、`patent/`（根仓库）

---

## 1. 结论先行

代码层面能做的已全部完成：仓库中不再存在可用的密钥默认值，缺失时一律安全兜底。

**但已经泄漏的凭据必须由你在云控制台吊销重签——任何代码改动都无法让已泄漏的密钥失效。**

---

## 2. 泄漏清单与处置状态

| # | 凭据 | 曾出现位置 | 是否进入 Git 历史 | 必须轮换 |
| --- | --- | --- | --- | --- |
| 1 | 百炼 DashScope API Key `sk-e4f0…8a9cc0` | `src/main/resources/application.properties`（已提交） | **是**，且已推送至 GitHub `origin/后端` | **是（最高优先级）** |
| 2 | DashScope 应用 ID `8f9c4f23…` `1f3bfccc…` | 同上 | **是** | 建议（见 §3.2） |
| 3 | 阿里云 OSS AccessKeyId / Secret `LTAI5t…` / `JIoM8Y…` | `Patent-Management-Platform-修改1/ai/.env` | 否（该文件被 .gitignore 排除） | **是**（见 §3.3） |
| 4 | 后台口令（弱口令） | `application.properties`（已提交）；`AdminAuthController` 代码默认值（**未提交**，该文件目前仍是未跟踪的新文件） | 仅经由 `application.properties` 进入历史 | **是**（已随本次改动强制移除，部署时必须设新口令） |
| 5 | JWT 签名密钥（用户端与管理端各有一个固定弱默认值，原文见 Git 历史） | `application.properties` | **是** | 是（已改为无默认值；本地已换成随机值） |

### 2.1 泄漏面评估

- 后端仓库的远程为 `git@github.com:Devliwen/Patent-Management-Platform.git`，密钥所在提交位于 `origin/后端` 分支（**不在** `main`、`frontend` 分支上）。
- 若该仓库为公开仓库，则 DashScope Key 应视为**已完全公开**，需立即吊销。若为私有仓库，风险取决于协作者范围，但仍建议轮换。
- OSS 凭据未进入任何 Git 历史，仅在本地 `.env` 文件中，泄漏面限于曾获得过该文件夹副本的人（打包分发、截图等）。

---

## 3. 待你完成的操作

### 3.1 吊销并重签 DashScope API Key（必做）

1. 登录 [阿里云百炼控制台](https://bailian.console.aliyun.com/) → 「API-KEY 管理」。
2. 删除旧 Key（`sk-e4f0…`）。
3. 新建 Key，写入 `patent/patent/config/application-local.properties` 的 `DASHSCOPE_API_KEY=`（该文件已被 .gitignore 排除）。
4. 同步检查该账号的用量与账单，确认没有异常调用。

> 为什么不能只改代码：Git 历史中的提交仍然存在，任何人检出该分支或用 GitHub 网页搜索都能拿到旧 Key。吊销是唯一彻底的处置方式。

### 3.2 DashScope 应用 ID（建议）

应用 ID 本身不能单独使用（调用仍需 API Key），但既然已公开，建议在控制台确认这两个应用没有异常调用记录；如需彻底隔离，可重建应用并替换 `DASHSCOPE_APP_ID` / `DASHSCOPE_APP_ID_PATENT`。

### 3.3 吊销并重签 OSS AccessKey（必做）

1. 登录阿里云 RAM 控制台 → 「用户」→ 找到对应 AccessKey（`LTAI5t…`）。
2. **先禁用**该 AccessKey，观察业务是否受影响，再删除。
3. 新建 AccessKey，更新到 `Patent-Management-Platform-修改1/ai/.env`。
4. 建议顺手收窄该 RAM 用户的权限：仅授予 `ihebut-patent` 这个 Bucket 的只读权限（该模块只做读取），避免 AK 泄漏导致数据被写坏。

> OSS 的风险高于 DashScope Key：AK/SK 可直接读写 Bucket 内容，影响面可能超出本平台。

### 3.4 后台口令与 JWT 密钥（部署前必做）

本地开发环境已自动配置（`patent/patent/config/application-local.properties`，且已被忽略）：

- `JWT_SECRET` / `ADMIN_JWT_SECRET` 已替换为随机 64 位十六进制串；
- `ADMIN_AUTH_PASSWORD` 暂保留了原先那个弱口令（原文见该文件，此处不重复记录）以便你无需重新记忆，**任何对外部署前必须改成强口令**。

正式部署请一律通过环境变量注入：

```bash
ADMIN_AUTH_PASSWORD=<强口令>
JWT_SECRET=<openssl rand -hex 32>
ADMIN_JWT_SECRET=<openssl rand -hex 32>
DASHSCOPE_API_KEY=<新签发的 Key>
```

---

## 4. 代码与仓库层面的改动（已完成）

### 4.1 后端 `patent/patent`

| 文件 | 改动 |
| --- | --- |
| `src/main/resources/application.properties` | 移除真实 DashScope Key 与应用 ID；`JWT_SECRET` / `ADMIN_JWT_SECRET` / `ADMIN_AUTH_PASSWORD` 默认值清空 |
| `security/JwtService.java` | **修复漏洞**：原先空密钥会派生出 `-admin` 补零的可预测管理端密钥；现改为回落随机密钥，并在密钥缺失时输出 WARN 日志 |
| `controller/AdminAuthController.java` | 移除代码中写死的默认口令（原文见 git 历史）；未配置口令时登录返回 **503「后台账号未配置」**（fail-closed） |
| `config/application-local.properties` | 真实凭据迁入此文件（已被 .gitignore 排除），并换用随机 JWT 密钥 |
| `config/application-local.properties.example` | 补充 `ADMIN_AUTH_PASSWORD` / `ADMIN_JWT_SECRET` 等必填项说明 |
| `src/test/resources/application.properties` | 新增测试专用固定凭据（不再依赖已删除的代码默认值） |
| `src/test/.../AdminAuthControllerTest.java` | 登录测试改用测试专用口令，不再硬编码原先那个弱口令 |
| `ai/.env.example` | 新增（README 早已引用该文件，此前并不存在） |

### 4.2 根仓库

| 文件 | 改动 |
| --- | --- |
| `.gitignore` | **新增**（此前根仓库没有任何忽略规则，这正是密钥副本能被暂存的原因）；覆盖构建产物、IDE 目录、`.env` 与本地配置、AI 模块缓存索引，放行 `*.example` 模板 |
| 暂存区 | 撤下 254 个不应入库的文件（835 → 581），含带密钥的 IDE 构建副本 `patent/output/`、`dump.rdb`、`__pycache__`、打包归档等 |
| `patent/output/`、`patent/patent/target/classes/application.properties` | 删除（构建产物中的明文密钥副本，已由本次改动消除） |

### 4.3 前端 `Patent-Management-Platform-修改1`

| 文件 | 改动 |
| --- | --- |
| `.gitignore` | 增加 `!.env.example` / `!**/.env.example`，否则模板文件会被 `ai/.env.*` 规则一并忽略 |
| `ai/.env.example` | 新增模板 |

### 4.4 文档

- `技术架构文档.md` §4.4 配置表：改为描述「未配置时的安全行为」，不再列出密钥默认值；§10 新增「已泄漏凭据」条目。

---

## 5. 验证方式

复查工作区是否还有残留（预期只命中下面两处，且均已被忽略）：

```bash
cd C:/Users/qiye0/Desktop/Patent_Platform

# 用各家凭据的特征前缀扫描，避免把密钥原文写进本文件
grep -rlE "sk-[a-f0-9]{32}" . --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=target
grep -rlE "LTAI[A-Za-z0-9]{12,}" . --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=target
# 预期：patent/patent/config/application-local.properties 与 .../ai/.env

# 如需核对具体某个历史值，从历史提交里取，不要写进任何文件：
#   git -C patent/patent log -p -S "<旧值片段>" -- src/main/resources/application.properties
```

确认这两个文件不会入库：

```bash
git check-ignore -v "Patent-Management-Platform-修改1/ai/.env"
cd patent/patent && git check-ignore -v config/application-local.properties
```

确认模板可以入库：

```bash
git check-ignore -v "Patent-Management-Platform-修改1/ai/.env.example"   # 应无输出
```

---

## 6. 遗留事项与已知限制

1. **JDK 17 已安装（构建阻塞已解除）**：项目要求 Java 17，而本机原只有 JDK 25，Lombok 1.18.32 在 JDK 25 下会以 `ExceptionInInitializerError: com.sun.tools.javac.code.TypeTag :: UNKNOWN` 失败（已用未修改的原始代码复现，确认与业务代码无关）。
   现已安装 **Temurin JDK 17.0.20.1 到 `D:\Java\jdk-17`**（免管理员权限的 ZIP 解压方式，与原有 `D:\Java\jdk`(25) 并存，删除该目录即可完全卸载）。
   验证结果：`mvn compile` 编译 147 个源文件成功；`mvn -DskipTests package` 产出 `target/patent-0.0.1-SNAPSHOT.jar`（71MB）。
   **注意：系统 `JAVA_HOME` 仍指向 JDK 25**，使用方式：
   - 命令行：先 `set JAVA_HOME=D:\Java\jdk-17`（或 Git Bash 下 `export JAVA_HOME=/d/Java/jdk-17`）再运行 Maven；
   - IDEA：`File → Project Structure → SDK` 添加 `D:\Java\jdk-17` 并设为项目 SDK，同时把 `Settings → Build Tools → Maven → Runner → JRE` 指到它（后端仓库尚未创建 `.idea`，首次打开时需要手动选）。
2. **单元测试当前无法通过（与本次密钥改动无关）**：8 个测试中 7 个在 Spring 上下文加载阶段失败，根因是未跟踪的新文件 `service/AdminRuntimeService.java` 在 `@PostConstruct` 中调用 `listColumns()`，执行了 MySQL 专有的
   `information_schema.COLUMN_TYPE` 查询，而测试用的是 H2 内存库，H2 不支持该列。HEAD 原始代码不包含此文件，测试因此可通过（5/5）。
   即：**这是仓库中未提交的重构中间态与 H2 测试环境的不兼容**，需要另行修复（可选方案：让 `listColumns` 兼容 H2、或在测试环境禁用数据集加载、或测试改用真实 MySQL）。
   受此影响，`AdminAuthControllerTest` 的改动仍**未经过测试执行验证**；其 fail-closed 行为已另行用直接构造实例的运行时测试确认（未配置口令时返回 503）。
3. **Git 历史未改写**：后端仓库的密钥仍在历史提交中（已推送至 GitHub）。考虑到强制推送会破坏协作者分支且无法从托管平台彻底抹除，本文建议以「吊销重签」作为主处置手段。如你希望额外清理历史，可使用 `git filter-repo` 后强制推送，并通知所有协作者重新克隆——这需要你来决定，我可以代做。
4. `Patent-Management-Platform-修改1/ai/` 目录没有 Python 源码（只有 `__pycache__`），代码实现位于 `patent/patent/ai/oss_qa/`；该目录下的 `cache/` 与 `index/` 是从 OSS 拉取的内部文档与已构建索引，现已加入忽略规则，但**已构建的索引只有这一份**，请勿误删。
5. 前端 `.env.development` / `.env.production` 因 `.env.*` 规则被一并忽略（其中并无密钥），新克隆的仓库需要手动补建这两个文件才能本地构建。
4. `Patent-Management-Platform-修改1/ai/` 目录没有 Python 源码（只有 `__pycache__`），代码实现位于 `patent/patent/ai/oss_qa/`；该目录下的 `cache/` 与 `index/` 是从 OSS 拉取的内部文档与已构建索引，现已加入忽略规则，但**已构建的索引只有这一份**，请勿误删。
5. 前端 `.env.development` / `.env.production` 因 `.env.*` 规则被一并忽略（其中并无密钥），新克隆的仓库需要手动补建这两个文件才能本地构建。
