# 高校知识产权运营服务平台 — 技术架构文档

> 文档版本：v1.0（2026-09-07）
> 适用范围：`patent`（Spring Boot 后端）、`Patent-Management-Platform-修改1`（Vue 3 前端）

---

## 1. 项目概述

**高校知识产权运营服务平台（产品名「我的知识产权」）**，面向高校、科研院所、企业及个人用户，提供专利与专家资源整合、智能匹配、专利价值评估、需求对接与成果转化等功能的综合服务平台。

### 1.1 目标用户

- **个人用户**：高校教师、学生、科研工作者、企业研发人员、知识产权从业者、创业者
- **机构用户**：高校科研管理部门、企业技术研发部门、知识产权服务机构
- **专家用户**：知识产权领域专家、学科技术专家、产业应用专家
- **系统管理员**：平台运营维护团队

### 1.2 核心功能

| 功能模块 | 说明 |
| --- | --- |
| 专利查询 | 关键词/分类号/申请人等多维检索，支持筛选、排序、收藏，基于 Elasticsearch |
| 专家资源 | 按研究领域、姓名、单位查询，专家认证申请与审核 |
| 需求管理 | 发布技术需求，状态流转：待匹配 → 已匹配 → 已对接 → 已完成 |
| 智能匹配 | 需求与专利 / 专家自动匹配，匹配结果可人工确认并持久化 |
| 价值评估 | 四维智能评分（技术/市场/法律/经济），生成评估报告与预测价值 |
| 成果转化 | 上传、管理知识产权成果，展示转化成功案例 |
| AI 助手 | 基于大模型的智能问答（含专利咨询专用通道），支持流式输出 |
| 内容管理 | CMS 文章发布、首页内容运维、数据大屏 |
| 系统管理 | 用户管理、数据同步任务、审计日志、SQL 控制台、专利数据集配置 |

---

## 2. 总体技术架构

### 2.1 技术栈总览

| 层次 | 技术选型 | 版本 |
| --- | --- | --- |
| 前端框架 | Vue 3（Composition API） | 3.5.x |
| 前端构建 | Vite + TypeScript | 4.5.x / 5.2.x |
| UI 组件库 | Element Plus（按需自动导入） | 2.13.x |
| 图表 | ECharts（数据大屏） | 6.0 |
| 富文本 | TipTap（CMS 文章编辑器） | 3.17 |
| HTTP 客户端 | Axios | 1.6 |
| 移动端封装 | Capacitor（Android） | 8.1 |
| E2E 测试 | Playwright | 1.59 |
| 后端框架 | Spring Boot | 3.2.2 |
| 语言 | Java | 17 |
| 持久化 | Spring Data JPA + MySQL | — |
| 检索引擎 | Elasticsearch（Spring Data Elasticsearch） | — |
| 安全 | Spring Security + JWT（jjwt 0.12.6） | — |
| 实时通信 | Spring WebSocket（原生 TextWebSocketHandler） | — |
| 大模型 | 阿里云百炼 DashScope（OpenAI 兼容模式，DeepSeek V3.2） | — |
| 构建工具 | Maven / npm | 3.12.1 |

### 2.2 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端                                 │
│   Web 浏览器 (PC / 平板)          Android App (Capacitor)     │
└───────────────┬─────────────────────────────┬───────────────┘
                │                             │
    Vite Dev (5173)  /api 代理  构建产物 dist (静态资源)
                │                             │
┌───────────────▼─────────────────────────────▼───────────────┐
│                 Spring Boot 后端 (patent, :8080)              │
│                                                              │
│  Controller 层  ── 24 个 REST 控制器 (/api/**)               │
│        │                                                     │
│  Service 层    ── 业务逻辑 / AI 调用 / ES 封装               │
│        │                                                     │
│  Mapper 层     ── Spring Data JPA Repository (30 个)          │
│        │                                                     │
│  Entity 层     ── 30 个实体 (MySQL)                          │
│                                                              │
│  SecurityFilterChain ── JwtAuthFilter (双 Token: USER/ADMIN)  │
│  WebSocketHandler   ── /ws/ai-chat (AI 流式对话)             │
└──────┬──────────────────────────────┬───────────────────────┘
       │                              │
┌──────▼──────┐              ┌────────▼────────┐
│    MySQL    │              │ Elasticsearch   │
│  patent 库  │              │ 索引: patents   │
│  29 张表    │              │ (5 类专利聚合)   │
└─────────────┘              └─────────────────┘
                │
┌───────────────▼───────────────┐
│ 阿里云百炼 DashScope           │
│ POST /compatible-mode/v1/...  │
│ model: deepseek-v3.2          │
└───────────────────────────────┘
```

### 2.3 关键设计决策

- **单体架构**：后端为单一 Spring Boot 应用，未拆分微服务，符合当前业务规模；分层清晰（Controller → Service → Mapper/Entity），为后续模块化演进留有余地。
- **双 Token 体系**：普通用户（JWT scope=USER）与管理后台（JWT scope=ADMIN）使用独立的签名密钥与过期配置，前端分别存于 `token` 与 `adminToken` 两个 localStorage 键。
- **MySQL 多表 + ES 聚合搜索**：专利原始数据按 5 大技术领域分表存储（wind/solar/biomass/hydrogen/lilon），ES 作为统一检索入口，支持增量/定向索引重建。
- **大模型集成**：后端直连 DashScope（OpenAI 兼容协议），WebSocket 承载流式响应；AI 聊天会话与消息持久化到 MySQL，保证可回溯。

---

## 3. 仓库结构

项目根目录包含两个独立仓库：

```
C:\Users\qiye0\Desktop\知识产权平台\
├── patent\                                    # Spring Boot 后端仓库
│   └── patent\
│       ├── pom.xml                            # Maven 构建配置
│       ├── config\                            # 本地覆盖配置 (gitignore 外)
│       │   ├── application-local.properties
│       │   └── application-local.properties.example
│       ├── src\main\java\org\ihebut\patent\patent\
│       │   ├── PatentApplication.java         # 启动入口
│       │   ├── config\                        # WebSocket 配置
│       │   ├── controller\                    # REST 控制器 (24 个)
│       │   ├── dto\                           # 请求/响应 DTO (41 个)
│       │   ├── entity\                        # JPA 实体 (30 个)
│       │   ├── mapper\                        # JPA Repository (30 个)
│       │   ├── search\                        # ES 文档/索引管理/启动初始化
│       │   ├── security\                      # JWT + Security 配置
│       │   ├── service\                       # 业务服务 (12 个)
│       │   └── websocket\                     # AI 聊天 WebSocket Handler
│       ├── src\main\resources\
│       │   ├── application.properties         # 主配置（环境变量可覆盖）
│       │   └── *_upgrade.sql                  # 增量升级脚本
│       ├── docs\                              # 数据库 schema、接口文档、演示 PPT
│       ├── ai\                                # AI 相关辅助脚本/材料
│       └── target\                            # 构建产物 (jar)
│
└── Patent-Management-Platform-修改1\           # Vue 3 前端仓库
    ├── package.json  vite.config.ts  tsconfig.json
    ├── capacitor.config.ts                     # Capacitor (Android) 配置
    ├── playwright.config.ts                    # E2E 测试配置
    ├── .env.development / .env.production
    ├── android\                                # Capacitor Android 工程
    ├── e2e\                                    # Playwright 测试用例 (4 个)
    ├── docs\                                   # 设计规范文档
    ├── public\  dist\  scripts\  skills\
    └── src\
        ├── main.ts  App.vue
        ├── api\        # axios 封装 + 接口模块 (index/admin/cms)
        ├── components\ # AIChat、CountTo、FeatureCard 等
        ├── composables\ # useScrollAnimation
        ├── constants\  # brand (平台名) / domain
        ├── content\    # 首页文案
        ├── lib\        # runtime (API 地址解析)、api-errors、markdown
        ├── router\     # 路由 + 全局守卫
        ├── styles\     # 主题 tokens / 全局样式
        ├── types\      # TS 类型定义 (与后端 DTO 对齐)
        └── views\      # 15 个主页面 + admin\ (9 个管理页)
```

---

## 4. 后端架构

### 4.1 依赖清单（pom.xml 要点）

- **Web 全家桶**：spring-boot-starter-web、websocket、data-jpa、data-elasticsearch、security、actuator
- **JWT**：jjwt-api / jjwt-impl / jjwt-jackson 0.12.6
- **数据库**：mysql-connector-j（runtime）
- **工具**：Lombok 1.18.32
- **测试**：spring-boot-starter-test、H2

> 注意：**未引入 Redis starter**。项目计划书虽规划了 Redis 缓存，但当前代码实现中未使用 Redis。（早期根目录曾误放一个与本项目无关的 `dump.rdb` 本地文件，已清理。）

### 4.2 分层结构

| 层 | 职责 | 数量 |
| --- | --- | --- |
| `controller` | 接收 HTTP 请求、参数校验、组装响应（统一 `ApiResponse<T>` 包裹） | 24 |
| `service` | 业务编排：AI 调用、ES 操作、统计聚合、审计记录等 | 12 |
| `mapper` | Spring Data JPA Repository 接口，单表 CRUD 与派生查询 | 30 |
| `entity` | JPA 实体，映射 MySQL 表 | 30 |
| `dto` | 请求/响应传输对象，与实体解耦 | 41 |
| `search` | ES 文档（`PatentSearchDocument`）、索引管理（`PatentEsIndexManager`）、启动初始化（`PatentEsStartupInitializer`）、IK 分词开关 | 5 |
| `security` | `SecurityConfig`（过滤链）、`JwtAuthFilter`、`JwtService`、`CurrentUser`（线程上下文）、`AppAuthPrincipal` | 5 |
| `websocket` | `AiChatWebSocketHandler`（WebSocket 文本消息处理） | 1 |

### 4.3 启动入口

`PatentApplication` 通过 `@SpringBootApplication(exclude = {ElasticsearchRepositoriesAutoConfiguration, ReactiveElasticsearchRepositoriesAutoConfiguration})` 启动——**显式排除 ES 的 Repository 自动化**：项目不使用 Spring Data ES 的 Repository 模式，而是通过 `ElasticsearchOperations` / 查询 DSL 手动封装检索（见 `PatentEsService`）。

### 4.4 配置体系（application.properties）

配置文件加载顺序：

```
application.properties
  └─ optional: classpath:application-secrets.properties
  └─ optional: file:./application-local.properties
  └─ optional: file:./config/application-local.properties
```

敏感项一律通过环境变量注入，仓库内配置文件**不提供任何可用的密钥默认值**。留空时的行为已做安全兜底（见右列），真实值请写入 `config/application-local.properties`（已被 `.gitignore` 排除）：

| 配置项 | 仓库内默认值 | 未配置时的行为 |
| --- | --- | --- |
| `DB_URL` | `jdbc:mysql://localhost:3306/patent` | MySQL，`ddl-auto=none`（手工执行 schema SQL） |
| `DB_USERNAME/DB_PASSWORD` | `root / root` | 数据库凭据（本地开发默认值，非敏感凭据） |
| `JWT_SECRET` / `JWT_EXP_MS` | 空 / 86400000 | 未配置则每次启动生成随机密钥，旧 Token 全部失效（用户 Token 24h） |
| `ADMIN_JWT_SECRET` / `ADMIN_JWT_EXP_MS` | 空 / 86400000 | 同上；**不会**由空密钥派生出可预测的管理端密钥 |
| `ADMIN_AUTH_USERNAME/PASSWORD` | `admin` / 空 | **后台登录直接返回 503「后台账号未配置」**（fail-closed，配置账号而非数据库） |
| `ES_URIS` | `http://localhost:9200` | ES 地址 |
| `ES_INDEX_PATENT` | `patents` | 专利索引名 |
| `ES_PREFER_IK` | `false` | 是否优先使用 IK 中文分词 |
| `ES_AUTO_CREATE_INDEX` | `true` | 启动时自动建索引 |
| `ES_AUTO_REINDEX` | `false` | 启动时自动全量重建索引 |
| `DASHSCOPE_API_KEY` | 空 | 调用 AI 接口时抛出「未配置通义千问 API Key」 |
| `DASHSCOPE_APP_ID` / `DASHSCOPE_APP_ID_PATENT` | 空 | 调用 App 通道时抛出「未配置通义千问 App ID」 |
| `DASHSCOPE_BASE_URL` | `https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions` | OpenAI 兼容接口 |
| `DASHSCOPE_MODEL` | `deepseek-v3.2` | 默认模型 |

> 历史遗留：`application.properties` 曾内置真实 DashScope Key 与应用 ID，已从工作区与暂存区清除，但**仍存在于后端仓库的历史提交中**，必须在百炼控制台吊销后重新签发。AI 检索模块（`ai/.env`）的 OSS 凭据同理。详见仓库根目录的密钥处置说明。

### 4.5 REST API 模块

统一响应格式：`ApiResponse<T> = { code, message, data }`（`ApiResponse.ok(...)` 成功、`GlobalExceptionHandler` 兜底异常）。

| 前缀 | 控制器 | 说明 |
| --- | --- | --- |
| `/api/auth/**` | AuthController | 用户注册、登录（含验证码 LoginCaptchaService） |
| `/api/admin/auth/**` | AdminAuthController | 管理后台登录（配置账号+独立 JWT） |
| `/api/users/**` | UserController | 用户信息、资料（`/api` 下另有 UserProfileUpdate 等） |
| `/api/patents` | PatentController | 专利多表查询（按 category 分发的 MySQL 查询） |
| `/api/patents/es` | PatentEsController | ES 全文检索：`/search`、状态、明细 |
| `/api/user-patents` | UserPatentController | 用户自有专利 CRUD |
| `/api/requirements` | RequirementController | 需求发布/列表/详情、**智能匹配**（match-patents / match-experts / persist） |
| `/api/experts` | ExpertProfileController | 专家档案、认证申请与审核 |
| `/api/organizations` | OrganizationController | 机构与成员管理 |
| `/api/valuations` | ValuationController | 专利价值评估报告生成/查询/参数维护 |
| `/api/transformations` | TransformationController | 成果转化记录管理 |
| `/api/chat` | ChatController | AI 对话会话 CRUD + `/ask`（阻塞式问答） |
| `/api/ai` | AiController | `/chat` 通用问答、`/chat/patent` 专利专用问答（OpenAI 兼容直连） |
| `/api/dashboard/screen` | DashboardController | 数据大屏聚合数据（分类统计、趋势、机构排名等） |
| `/api/notifications` | NotificationController | 站内通知 |
| `/api/public/presence` | PresenceController | 在线状态心跳/统计 |
| `/api/sync-jobs` | SyncJobController | 数据同步任务记录 |
| `/api/admin` | AdminRuntimeController | 数据源发现（表/列）、数据集定义与重建、**SQL 控制台** |
| `/api/admin` | AdminCmsController | CMS 文章管理 |
| `/api/cms` / 公开 GET | CmsController | 文章列表/详情、首页内容 |
| `/api` | ApiIndexController | 健康探测：`GET /api` |
| `/api/audit-logs` 等 | AuditLogController | 审计日志查询 |

**安全矩阵**（SecurityConfig）：公开端点 = 认证接口、`/ws/**`、`/api/ai/**`、`/api/public/**`，以及对专利/专家/需求/机构/CMS/数据大屏的 GET 只读接口；**其余均需 JWT**。POST/PUT/DELETE 一律要求已认证。

### 4.6 认证与鉴权

- **双 Token**：`JwtService.createUserToken(userId)` 签发 scope=`USER` 的 JWT；`createAdminToken(username)` 签发 scope=`ADMIN` 的 JWT。两套密钥与有效期独立。
- **过滤器**：`JwtAuthFilter`（OncePerRequestFilter）从 `Authorization: Bearer` 解析 Token → `parseAuthPrincipal` 判定 scope → 组装 `AppAuthPrincipal` 放入 SecurityContext；控制器/服务经 `CurrentUser` 静态工具获取当前用户 ID。
- **无状态**：`SessionCreationPolicy.STATELESS`，无 CSRF（API 使用 Bearer Token）。
- **密码存储**：BCryptPasswordEncoder。
- **后台鉴权**：AdminAuthController 直接对比配置的 `admin.auth.username/password`（配置来源），仅管理端接口受保护。

> ⚠️ 安全提示：内置默认密钥/账号/API Key 均为演示值，正式部署必须通过 `ADMIN_AUTH_PASSWORD`、`JWT_SECRET`、`DASHSCOPE_API_KEY` 等环境变量覆盖；`/api/admin/sql/execute`（SQL 控制台）风险较高，仅限可信内网环境开放。

### 4.7 数据库设计（MySQL，库名 `patent`）

Admin 侧通过 `dba` 模式执行 `docs/database_schema.sql` 等脚本初始化，`ddl-auto=none`。共 29 张表，按业务分组：

**用户与权限**：`user_account`（账号）、`user_profile`、`user_role`、`role`、`permission`、`role_permission`、`user_organization`、`organization`

**专家**：`expert_profile`（`cert_status`：PENDING/AUDITED，认证状态机）

**专利数据**（5 大类分表，结构一致）：
- `patent_wind` / `patent_solar` / `patent_biomass` / `patent_hydrogen` / `patent_lilon`
- 核心字段：publicNum、title、abstractText、applicant、inventor、ipc/cpc/nec（分类号）、legalStatus、appliDate、publicDate、patentDetails（全文详情）

**用户自有专利**：`user_patent`（与外部专利表同构，归属 `ownerUserId`）

**需求与匹配**：`requirement`（status：PENDING/MATCHED/CONTACTED/COMPLETED，含预算 budget、合作方式、技术方向）、`requirement_patent_match`、`requirement_expert_match`（匹配快照，含 matchScore/matchReason）

**价值评估**：`patent_valuation_report`（四维评分 + `reportJson` 冗余展示结构 + predictedValue）、`patent_valuation_model_param`（模型权重参数）

**成果转化**：`transformation_result`（关联专利/专家/需求/机构，benefitAmount 收益）

**AI 对话**：`chat_session`、`chat_message`

**运营**：`cms_article`、`home_content`（首页配置）、`user_notification`、`data_sync_job`、`audit_log`

增量升级脚本（`src/main/resources/`）：`audit_log_upgrade.sql`、`requirement_contact_budget_upgrade.sql`、`valuation_dataset_upgrade.sql`

### 4.8 Elasticsearch 检索

- **文档模型**：`PatentSearchDocument`（index=`patents`），字段映射 MySQL 专利表 + 数据集归属（datasetId/datasetName/recordId/category）。
- **索引生命周期**：
  - `PatentEsIndexManager`：创建/删除索引、mapping（配合 IK 分词开关）
  - `PatentEsStartupInitializer`：启动时按 `ES_AUTO_CREATE_INDEX` / `ES_AUTO_REINDEX` 自动建索引或重建
  - `PatentEsService.reindexAll / reindexCategory(category) / reindexDataset(datasetId) / indexOne`：全量/分域/单条索引
- **查询能力**：`search`（多字段 match + phrase 模式）、按数据集检索 `searchByDataset`、明细 `findDetail`；管理端的"数据集"定义（`AdminPatentDatasetDefinition`）支持从**任意 MySQL 数据源的表** 动态映射文档字段（JDBC 读取表结构 + `mapRowToDoc`），实现异构专利库的接入。
- 检索入口：`PatentEsController /api/patents/es/**`；兜底入口：`PatentController`（MySQL 多表直接查询）+ `PatentTableService`（按 category 分发到对应分表）。

### 4.9 核心业务逻辑

**(1) 智能匹配（RequirementController）**
- `GET /api/requirements/{id}/match-patents`：在用户专利 + 外部专利（包含 ES 检索）中按需求关键词/技术方向推荐，返回 `PatentMatchResult`（含 matchScore）
- `GET /api/requirements/{id}/match-experts`：按 `expert_profile.field / expertise` 模糊匹配专家
- `POST /{id}/match-patents/persist` / `match-experts/persist`：**人工确认后**将候选结果落库为匹配快照（upsert，重复项更新分值）

**(2) 专利价值评估（ValuationController）**
- 评估对象：外部专利（按 datasetId+recordId 或 publicNum 从 ES 取）或用户专利（userPatentId）
- 方法：**启发式四维评分**（`smart-heuristic`，0~100 分）：
  - `computeTechnologicalInnovationScore` 技术创新度
  - `computeMarketPotentialScore` 市场潜力
  - `computeLegalStatusScore` 法律状态（由标题/摘要关键词推断）
  - `computeEconomicValueScore` 经济价值
  - 综合分 = Σ(维度分 × 权重) / 100，权重默认各 25%（`patent_valuation_model_param` 可调、请求内可覆盖）
  - `predictedValue = overallPercent% × 100 万元`
- 报告以 `reportJson` 落库，旧字段按 0~1 兼容保存；响应 `ValuationReportResponse` 包含四维分数、结论、专利信息。

**(3) AI 对话（DashScopeChatService + ChatService）**
- 直连 DashScope OpenAI 兼容端点：`POST {base-url}`（model 默认 `deepseek-v3.2`，可覆盖；`temperature`/`maxTokens` 可调）
- 双通道：`chatWithApp(...)` 通用问答（app-id）、`chatWithPatent(...)` 专利咨询（app-id-patent），可携带会话上下文
- `ChatService.ask / streamAsk`：组装历史消息 → 调模型 → 持久化 `chat_session / chat_message` → 会话首问自动生成标题
- **流式**：WebSocket 通道逐 token 下发（见 4.10）

**(4) 数据大屏（DashboardController + DashboardService）**
- `GET /api/dashboard/screen`：聚合专利分类统计、转化趋势、数据集排名、专家领域分布、最新转化成果；对部分统计源做了「安全降级」（`safeCount / safeFetch`），数据源不可用时返回 0 而非报错。

**(5) CMS 与首页运营**：`cms_article`（TipTap HTML 内容）、`home_content`（首页 Hero/卡片配置，前端 `content/homeCopy.ts` 兜底文案）。

**(6) 管理后台**：专利数据源发现（`/patent-sources/*/tables/columns`）、数据集 CRUD 与重索引、SQL 控制台（含 SQL 历史）、审计日志（`AuditLogService` 记录关键写操作）。

### 4.10 WebSocket（AI 流式聊天）

- 端点：`/ws/ai-chat` 与 `/ws/ai/chat` 两路径注册同一 Handler，`setAllowedOrigins("*")`
- 认证：`AiChatWebSocketHandler.afterConnectionEstablished` 从查询参数/请求解析 JWT（`resolveUserId`），未登录直接 `CloseStatus.NOT_ACCEPTABLE`
- 消息协议（JSON）：
  - 客户端 → 服务端：`{ "type": "chat.ask", "sessionId": "...", "question": "..." }`
  - 服务端 → 客户端事件：`session.created`、`answer.delta`（流式增量）、`answer.done`（结束，含完整答案/模型/requestId）、`answer.error`
- 前端 `AIChat.vue` 组件通过 `resolveWsBaseUrl()` 建立连接并渲染流式回复。

---

## 5. 前端架构

### 5.1 构建与工程配置

- **Vite 4**，`@` 别名指向 `src/`；`unplugin-auto-import` + `unplugin-vue-components` + ElementPlusResolver 实现 API 与组件的按需自动导入（`auto-imports.d.ts` / `components.d.ts` 为自动生成）
- **分包策略**（vite.config.ts `manualChunks`）：`vendor-echarts`、`vendor-element-plus`、`vendor-editor`（tiptap）、`vendor-core`（vue/router/axios）、`vendor-misc`
- **SPA History 兜底**：dev server 自定义中间件将非资源/非 API 的 GET 请求重写为 `/index.html`
- **开发代理**：`/api` → `http://127.0.0.1:8080`（可用 `VITE_API_PROXY_TARGET` 覆盖）、`/ws` → `ws://127.0.0.1:8080/ws/ai-chat`（`ws: true`），端口 5173
- **环境变量**：`.env.development` / `.env.production` 均定义 `VITE_API_BASE_URL=/api`、`VITE_APP_TITLE`、`VITE_AI_MODEL=deepseek-v3.2`

### 5.2 入口与状态管理

- `main.ts`：加载 Element Plus 样式、全局主题（`data-theme` 属性，默认 light）、`createApp(App).use(router)`
- **无 Pinia/Vuex**：全局状态以 `localStorage` + 组件内 `ref`/`composable` 为主：
  - 用户 Token：`localStorage.token`；管理 Token：`localStorage.adminToken`
  - 主题偏好：`localStorage.theme`（`ThemeSwitcher.vue`）
  - 运行时 API 地址：`localStorage.runtime.apiOrigin`（原生 App 用）

### 5.3 路由与权限守卫（src/router/index.ts）

| 路由 | 页面 | 鉴权 |
| --- | --- | --- |
| `/` | Home 首页 | 公开 |
| `/data-screen` | 数据大屏 | 公开 |
| `/patent` | 专利检索 | 公开 |
| `/requirement` | 需求广场 | 公开（发布需登录） |
| `/articles/:slug` | 文章详情 | 公开 |
| `/login` `/register` | 登录/注册 | 已登录则跳首页 |
| `/demand` | 需求发布 | `requiresAuth` |
| `/evaluation` | 价值评估 | `requiresAuth` |
| `/patent/manage` | 专利管理 | `requiresAuth` |
| `/expert` | 专家资源 | `requiresAuth` |
| `/transformation` | 成果转化 | `requiresAuth` |
| `/valuation` | 评估报告 | `requiresAuth` |
| `/profile` | 个人中心 | `requiresAuth` |
| `/admin/login` | 后台登录 | 单独 adminToken 判断 |
| `/admin/**` | 管理后台（Layout + 子路由） | `requiresAdminAuth`：首页配置、文章管理/编辑、专利数据源、专利数据集、SQL 控制台 |
| `/admin/audit-logs` | 审计日志 | `requiresAdminAuth` |

守卫逻辑：`router.beforeEach` 设置 `document.title`，校验 `token`/`adminToken`，未登录跳登录页并携带 `redirect` 回跳；其余兜底到首页。

### 5.4 API 层（src/api）

- **封装**：`ApiService` 类持有 axios 实例（`baseURL = resolveApiBaseUrl()`，超时 180s）；双层响应拦截器：请求拦截注入 `Authorization: Bearer`（白名单接口除外）+ Token 状态预检；响应拦截解析统一信封 `ApiResponse`，错误映射 `lib/api-errors.ts` → Element Plus 消息/确认框
- **Token 管理**：解析 JWT payload（本地 `atob`）检测过期与「5 分钟内到期」状态，状态变化才提醒；已过期则清理并跳登录（避免频繁弹窗，5s 节流缓存）
- **请求控制**：相同 URL 的 CancelToken 去重（重复请求取消）、响应缓存 Map
- **模块划分**：`api/index.ts`（业务接口，全量函数导出）、`api/admin.ts`（后台接口：数据集/数据源/重建索引/SQL）、`api/cms.ts`（内容管理）
- **运行时地址解析** `lib/runtime.ts`（**Web / Native 双环境**）：
  - Web：`VITE_API_BASE_URL`（默认 `/api` 相对路径，走 Vite 代理）；WS 用当前页面协议 `ws(s)://host`
  - Capacitor 原生：使用 `localStorage.runtime.apiOrigin` → `VITE_NATIVE_API_ORIGIN` → 代码内默认值。**注意 `runtime.ts` 内的默认值目前仍是早期的 `http://60.205.242.113:8080`，该地址早已不可达**；当前生产后端为 `hebut-ip.com`（`42.193.99.126`），打 Android 包前需同步修改 `runtime.ts` 与 `android/.../network_security_config.xml`。WS 由 http(s) 自动转 ws(s)
- **类型**：`src/types/*.ts` 与后端 DTO 对齐（Pageable、ApiResponse、ValuationReport 等）

### 5.5 页面与组件

**核心视图（views/）**：

| 文件 | 功能 |
| --- | --- |
| `Home.vue` | 门户首页（Hero/功能导航/成果案例，数据来自 home-content + 案例列表，含骨架屏） |
| `DataScreen.vue` | 数据大屏（ECharts：专利趋势/分类/机构排名/专家领域/转化案例） |
| `PatentSearch.vue` | 专利检索（条件筛选、结果表格、详情、分页） |
| `PatentManage.vue` | 用户专利管理（录入/编辑/删除） |
| `DemandPublish.vue` | 需求发布表单 |
| `RequirementManage.vue` | 需求广场（列表/详情/智能匹配 + 人工确认持久化） |
| `ExpertManage.vue` | 专家资源（列表/认证申请/审核） |
| `Evaluation.vue` | 价值评估（评估专利选择 → 权重 → 报告展示） |
| `ValuationManage.vue` | 评估报告管理（列表/详情） |
| `TransformationManage.vue` | 成果转化记录管理 |
| `Profile.vue` | 个人中心（资料修改） |
| `Login.vue` / `Register.vue` | 认证页 |
| `ArticleDetail.vue` | CMS 文章详情（Markdown/HTML 渲染 + DOMPurify 消毒） |

**管理视图（views/admin/）**：`AdminLayout`（后台框架）、`AdminLogin`、`AdminHomeContent`（首页运营配置）、`AdminArticles` + `AdminArticleEditor`（TipTap 编辑器）、`AdminPatentSources`（数据源发现）、`AdminPatentDatasets`（数据集/索引重建）、`AdminSqlConsole`（SQL 执行 + 历史）、`AdminAuditLogs`

**通用组件（components/）**：`AIChat.vue`（WebSocket 流式 AI 对话弹窗）、`CountTo`（数字滚动）、`FeatureCard`、`HomeSkeleton`、`ThemeSwitcher`（明暗主题）

**样式设计**：`styles/tokens` 主题变量（`data-theme` 切换）、`DESIGN_SYSTEM.md` 设计规范、`docs/` 品牌视觉规范与 WCAG 2.1 对比度报告。

---

## 6. 移动端（Capacitor）

- `capacitor.config.ts`：appId `com.myip.platform`、appName `我的知识产权`、webDir `dist`；Android 允许混合内容与明文 HTTP（开发便利，生产建议 https）
- 工作流：`npm run android:build`（= vue-tsc 构建 + `cap sync android`）→ Android Studio 打包
- 原生环境 API 地址通过 `lib/runtime.ts` 的 **运行时覆盖**（`localStorage.runtime.apiOrigin`）支持「设置页切服务器」；代码内默认值仍是早期地址 `http://60.205.242.113:8080`（已不可达），当前生产后端为 `hebut-ip.com`

---

## 7. 测试

**后端**：JUnit + Spring Boot Test + H2（`src/test`），Spring Security 单元测试针对登录/授权流程
**前端 E2E**（Playwright，`e2e/`）：

| 用例 | 覆盖 |
| --- | --- |
| `ui-smoke.spec.ts` | 页面加载冒烟 |
| `patent-search.spec.ts` | 专利检索交互 |
| `requirement-marketplace.spec.ts` | 需求广场/匹配流程 |
| `patent-ai-normalization.spec.ts` | 专利 AI 咨询归一化 |

- 配置：`playwright.config.ts` 双工程（Desktop Chrome + Pixel 5 移动端），`webServer` 自动执行 `npm run build && npm run preview`（4173），失败自动截图/trace；`npm run test:e2e` / `npm run test:e2e:ui`

---

## 8. 构建与部署

### 8.1 本地开发

```powershell
# 后端（需 MySQL patent 库 + Elasticsearch :9200）
cd patent\patent
copy config\application-local.properties.example config\application-local.properties   # 填本机真实 DB/ES 配置
mvn spring-boot:run

# 前端
cd Patent-Management-Platform-修改1
npm install
npm run dev        # http://localhost:5173，/api 与 /ws 自动代理到 8080
```

### 8.2 生产构建

```powershell
# 后端
mvn -DskipTests package
java -jar target/patent-0.0.1-SNAPSHOT.jar
# 环境变量注入：DB_URL/DB_USERNAME/DB_PASSWORD/JWT_SECRET/DASHSCOPE_API_KEY/ADMIN_AUTH_PASSWORD...

# 前端
npm run build      # 产物 dist/ 可直接由 Nginx/静态服务器托管；API 走同源 /api（或运行时配置）
npm run android:build  # 移动端：构建 + Capacitor sync
```

当前**无 Dockerfile / docker-compose**，部署以「jar + 静态资源」手工方式为主；`Patent-Management-Platform-修改1/` 下的 `frontend-dist.tar.gz`、`dist-upload.zip` 为历史打包产物。

### 8.3 启动依赖与前序排查结论（后端 README）

- 后端默认连接本机 MySQL（`root/123456` 演变为 `root/root`，以 `application-local.properties` 为准）与 ES `localhost:9200`，**任一不可用则启动失败**
- 历史问题：本地配置丢失 → 数据库凭据不匹配无法启动；前端曾指向远程 IP、WebSocket 路径不一致 → 均已修复（当前走本地代理）
- `.gitignore` 覆盖 `application-local.properties` 等本地敏感配置，改配置不会污染仓库

---

## 9. 关键链路示例

### 9.1 AI 流式对话时序

```
AIChat.vue ──(1) WebSocket 连接 /ws/ai-chat?token=<jwt>──▶ AiChatWebSocketHandler
         ◀──(2) {type:"session.created", sessionId}──    (首问自动建会话/标题)
     ──(3) {type:"chat.ask", question}──▶
         ◀──(4) {type:"answer.delta", delta:"..."}  × N   ChatService.streamAsk
         ◀──(5) {type:"answer.done", answer, model}       → DashScopeChatService(流式)
                                    后端同时持久化 chat_session / chat_message
```

### 9.2 专利检索链路

```
Question: 平台查询 → GET /api/patents/es/search?category=solar&query=...
  → PatentEsService.buildQuery（match 多字段 / phrase 模式，IK 可选）
  → ES patents 索引 → PatentEsSearchResponse（高亮/分页/条数）
兜底：GET /api/patents?category=... （MySQL 分表直查）
后台：管理端"数据集→重建索引"触发 PatentEsService.reindexDataset → JDBC 读源表 → 写 ES
```

### 9.3 价值评估链路

```
Evaluation.vue → POST /api/valuations {patentSource: EXTERNAL|USER, ...}
  → (EXTERNAL) ES findDetail 取专利 / (USER) user_patent 取专利
  → 四维启发式评分（关键词+摘要）→ 按权重加权（默认 25%×4）
  → predictedValue = 综合分% × 100万 → 报告落库(reportJson)
  → 前端渲染四维雷达图/结论
```

---

## 10. 已知问题与改进建议

| 项 | 现状 | 建议 |
| --- | --- | --- |
| 敏感默认值 | 已修复：仓库内配置不再含可用的密钥默认值，缺失时安全兜底（JWT 随机密钥 / 后台登录 fail-closed） | 真实值经环境变量或 `config/application-local.properties` 注入 |
| 已泄漏凭据 | DashScope Key/应用 ID 与 OSS AK/SK 曾进入 Git 历史（OSS 凭据位于 `ai/.env`） | **必须吊销重签**；代码改动无法让已泄漏凭据失效，见根目录《密钥处置说明.md》 |
| Redis 未落地 | 计划书规划 Redis，代码未引入 | 高频读接口（专利检索、大屏）可加缓存层 |
| SQL 控制台 | `/api/admin/sql/execute` 执行任意 SQL | 限制 DML/DDL、脱敏、内网白名单 |
| 后端缺少统一 Service 层 | 多处控制器直接注入 Mapper（如 ExpertProfile/Organization/Requirement） | 逐步下沉业务逻辑至 Service，便于测试 |
| ES 与 MySQL 一致性 | 双索引重建为主，无 CDC 同步 | 关注 `ES_AUTO_REINDEX=false` 时的数据更新链路 |
| 专利分类固定 | 五大领域（风/光/生物质/氢/锂）写死于 `VALID_PATENT_CATEGORIES` | 如有新领域需同步前后端枚举与建表脚本 |
| API 文档 | `docs/api_v7_项目书全量接口文档.md`、Postman 集合 | 建议引入 springdoc-openapi 自动生成 |

---

*附：本文档基于 2026-09-07 仓库状态整理，涉及文件：`patent/patent`（后端）、`Patent-Management-Platform-修改1`（前端）。*
