# 面向「专利—需求」精确匹配的垂直场景智能体系统

> 参赛赛道：人工智能在垂直应用场景的创新设计与开发
> 选题落点：高校知识产权运营服务平台的**智能匹配精确度提升**
> 文档性质：设计方案（含现状实测、技术路线、评测体系、实施计划、演示案例）

---

## 0. 一页速览

**做什么**：把平台上「企业发技术需求 → 匹配高校专利」这一环，从"把需求丢给大模型自由发挥"升级为**可量化、可解释、可校核的四阶段匹配流水线**，并用自建评测集证明精确度提升。

**为什么是垂直场景**：专利文本不是普通文本。它有权项结构、IPC/CPC 层级分类号、法律状态、同族专利、时间属性；"匹配得上"不等于"语义相似"。通用 RAG 或通用语义相似度在这类任务上会系统性地给出"看着相关、实际不可用"的结果（例如推荐了一件已失效专利，或推荐了技术领域相同但技术手段完全不同的方案）。**领域结构化 + 可验证**才是这类场景的正解——这正是本方案的设计内核。

**核心改动**：现状是"大模型凭记忆报专利号 + 正则提取"；方案改为
`需求结构化(AIGC) → 混合召回(BM25+kNN) → 领域特征精排 → LLM 重排与理由生成(AIGC) → 校核 Agent 拦截幻觉`，
并保留人工确认作为最终裁决。

**可证明性**：建立标注评测集，报告 Precision@5、nDCG@10、幻觉率相对基线的变化，并做消融实验说明每个模块的贡献。竞赛评委最认这一点：**不是"我们用了大模型"，而是"我们能证明精确度从 X 提到 Y，且知道每一分是谁贡献的"**。

---

## 1. 选题与场景

### 1.1 业务背景

平台连接两类主体：

- **需求方**：企业/机构，用自然语言描述技术需求（"我们需要提高氢燃料电池催化剂的耐久性，目标 5000 小时以上"）。
- **供给方**：高校/科研院所的专利与专家。

匹配是整条转化链路的**咽喉环节**：匹配错了，后面的评估、对接、转化全部作废。因此"匹配精确度"不是体验优化，而是决定平台价值能否成立的核心指标。

### 1.2 场景的垂直特殊性（这是赛道评审看重的"为什么不能直接套通用方案"）

| 专利文本特性 | 对匹配算法的硬性要求 |
| --- | --- |
| 技术特征分散在标题/摘要/权利要求中，且用词高度专业化 | 需要领域语义表征，纯关键词必然漏召 |
| IPC/CPC 是**层级化**分类号（如 H01M → H01M4/90） | 分类号相似度需按层级计算，不能当普通字符串 |
| 法律状态直接影响可用性（失效专利不可转化） | 必须作为**硬过滤或强权重**参与排序 |
| 存在同族专利（同一技术多国申请） | 需要去重，否则一屏全是同一件技术 |
| 时间属性敏感（技术时效性） | 需要时间衰减因子 |
| 技术需求常有隐含约束（成本、工况、排除项） | 需要结构化抽取，而非直接向量化整段文本 |

**结论**：通用语义相似度只能解决"召回"，解决不了"这个场景下到底该不该推荐它"。方案必须同时做**语义召回**与**领域规则精排**，两者缺一不可。

---

## 2. 需求分析

### 2.1 现有实现实测（基于当前代码，非推测）

现有"智能匹配"的真实链路如下（已逐行核对代码）：

```
RequirementManage.vue  L461-501
  └─ 取 需求标题 + 描述 拼成 requirement 文本
  └─ POST /api/ai/chat/patent        ← 注意：不是 GET /requirements/{id}/match-patents
        │
AiController.chatWithPatent()  L68+
  ① buildPatentSearchPrompt()  L136-149
       提示词原文：「你是专利检索助手…优先识别并列出尽可能多的相关专利公开号」
       → 让大模型凭【参数记忆】回忆专利号，没有任何检索步骤
  ② chatWithApp(..., usePatentAppId=true)    调用百炼云端应用
  ③ extractPublicNums(aiContent)  L150
       → 用正则从大模型生成的散文里抠出专利公开号
  ④ patentEsService.findByPublicNums(nums)   ES 精确查号
  ⑤ 若结果 < MAX_AI_PATENT_RESULTS(=100)
       → ES 全文兜底，查询串 = requirement + " " + aiContent（把整个大模型回答拼进查询）
  ⑥ 返回：aiAnalysis(原始文本) / extractedPublicNums / patents / notFoundPublicNums
```

### 2.2 精确度问题的六条归因

| # | 问题 | 证据 | 对精确度的影响 |
| --- | --- | --- | --- |
| 1 | **检索缺失，靠大模型记忆报号** | 提示词要求"列出尽可能多的相关专利公开号"，无任何检索注入 | 根本性缺陷：模型对具体专利号的记忆不可靠，必然产生幻觉 |
| 2 | **幻觉专利号被显式记录却无处理** | 返回体里存在 `notFoundPublicNums` 字段 | 系统已知推荐了不存在的专利，但仅记录、不过滤、不告警 |
| 3 | **无评分、无排序** | 返回 `MAX_AI_PATENT_RESULTS = 100` 条，顺序即拼装顺序 | 用户面对 100 条平铺结果，无法判断优劣，等于没有排序能力 |
| 4 | **评分字段存在但恒为 NULL** | `RequirementPatentMatch.matchScore/matchReason` 存在；但 `PatentMatchResult` DTO **无 score 字段**，前端也从未构造 score（`RequirementManage.vue` L538-543 只填 4 个键） | **精确度当前无法量化**，没有基线就没有改进空间可证明 |
| 5 | **兜底查询引入噪声** | `fallbackQuery = requirement + " " + aiContent` | 把大模型的长篇回答当检索词，稀释真实意图权重 |
| 6 | **领域特征完全未用** | 匹配链路未使用 `legalStatus`（法律状态）、`applDate`（申请日）、IPC 层级 | 一致性检索、时效性、失效专利过滤全部缺失 |

补充：后端的 `GET /requirements/{id}/match-patents`（按 keywords 在 5 张 MySQL 分表做 `LIKE %整串%`）在前端**已无调用方**，属遗留死代码；其实现本身也无法分词（整串关键词做子串匹配）。

### 2.3 精确度无法提升的结构性原因

一句话总结：**当前系统里"精确度"这个量根本不存在**——没有分数、没有排序、没有标注集、没有指标。因此本方案的第一步不是换模型，而是**先把精确度变成可测量的量**。

### 2.4 量化目标（需先测基线，再定提升目标）

| 指标 | 定义 | 用途 |
| --- | --- | --- |
| Precision@5 / @10 | 前 5/10 条中强相关占比 | 用户第一屏体验，最直观 |
| Recall@50 | 强相关专利在召回集中的覆盖率 | 召回阶段能力上限 |
| nDCG@10 | 考虑排序位置的相关性增益 | 排序质量综合指标 |
| MRR | 首个强相关结果的平均倒数排名 | 首条命中能力 |
| **幻觉率** | 推荐的专利号在库中不存在的比例 | **本方案针对性指标**，现状必然非零 |
| 平均匹配理由可用率 | 人工判定"理由成立"占比 | 可解释性是否真实可信 |

> 目标值不在方案阶段臆定。**先跑基线评测得到现状数字，再据此设定提升目标**——这种"指标驱动"的表述在答辩时比空喊"提升 30%"更可信，也更容易防守。

---

## 3. 设计思路

### 3.1 总体架构：五阶段流水线

```
              需求（自然语言）
                    │
      ┌─────────────▼─────────────┐
      │ ① 需求结构化（AIGC 抽取）   │  RequirementProfileService
      │  技术问题/技术手段/预期效果/  │  → 结构化「技术画像」
      │  性能指标/应用场景/排除项/    │
      │  IPC 候选/时效要求          │
      └─────────────┬─────────────┘
                    │ 结构化画像 + 原始文本
      ┌─────────────▼─────────────┐
      │ ② 混合召回 Hybrid Recall   │  HybridRecallService
      │  ├─ 稀疏：ES BM25 multi_match（已有，含字段加权）
      │  └─ 稠密：ES dense_vector kNN（新增，语义召回）
      │  └─ RRF 融合 + 同族去重        │
      └─────────────┬─────────────┘
                    │ Top-100 候选
      ┌─────────────▼─────────────┐
      │ ③ 领域特征精排              │  PatentRankingService
      │  IPC 层级相似度 / 法律状态    │
      │  时间衰减 / 机构亲和 / 排除项  │
      └─────────────┬─────────────┘
                    │ Top-30
      ┌─────────────▼─────────────┐
      │ ④ LLM 重排 + 理由生成(AIGC) │  MatchExplainService
      │  技术特征对比表：对齐点/差异点 │
      │  分维度得分 + 置信度          │
      └─────────────┬─────────────┘
                    │ Top-10（带理由）
      ┌─────────────▼─────────────┐
      │ ⑤ 校核 Agent（防幻觉）      │  MatchAuditAgent
      │  逐条回溯 ES 校验存在性/       │
      │  法律状态/理由引用是否属实      │
      └─────────────┬─────────────┘
                    │
              人工确认（保留最终裁决权）
                    │
              反馈入库 → 迭代优化
```

### 3.2 阶段① 需求结构化——为什么必须放在最前面

现状把「标题+描述」整段直接向量化/直接喂模型，这是精确度上不去的第一原因：**一段需求描述里混着技术问题、指标要求、商务信息、口语化表述**，直接匹配会被噪声主导。

本阶段用 LLM 做结构化抽取（AIGC），产出「技术画像」：

```json
{
  "techProblem": "提升氢燃料电池催化剂在启停工况下的耐久性",
  "technicalMeans": ["铂基合金催化剂", "载体抗腐蚀改性"],
  "expectedEffect": ["5000 小时以上寿命", "性能衰减 < 10%"],
  "applicationScenario": "车用燃料电池电堆",
  "exclusions": ["不涉及膜电极结构变更"],
  "ipcCandidates": ["H01M4/92", "H01M4/90", "B01J23/42"],
  "timeliness": { "preferAfterYear": 2018 },
  "domain": "hydrogen"
}
```

要点：
- **AIGC 抽取 + 人工可编辑**：前端展示抽取结果，允许用户修正（这一步同时产出标注数据）。
- `ipcCandidates` 由 LLM 生成**候选**分类号，仅作为精排阶段的加分项，不作为硬过滤——避免 LLM 误判 IPC 直接掐死召回。
- `exclusions` 是现状完全缺失的能力，直接对应"推荐了不该推荐的"这类精确度损耗。

### 3.3 阶段② 混合召回——稀疏 + 稠密互补

**稀疏腿（已有，直接复用）**：`PatentEsService.buildQuery()` 中已实现带字段权重的 `multi_match`：

```
public_num^6, title^4, abstract^2, applicant^1, inventor^1,
patent_details^1, ipc^1, cpc^1, legal_status^1, all_text
```

**稠密腿（新增）**：对 ES 索引 `patents` 的 `all_text` 字段（已是全字段拼接，天然适合做向量化）生成 embedding，映射为 `dense_vector` 字段，用 kNN 检索。

**为什么选 ES 而不是引入新向量库**：
- 环境里已经有 ES **8.12.2**（`docs/elasticsearch/docker-compose-no-ik.yml`），8.x 的 `dense_vector` + kNN 已生产可用；
- 零新增基础设施，部署不变，对竞赛演示环境友好；
- 稀疏与稠密在同一引擎内可做融合，避免跨系统一致性问题；
- 项目已有 `PatentEsIndexManager`（建索引/mapping）与 `PatentEsService.reindexAll/reindexDataset`（重建）基建，**扩字段 + 重建即可**，复用度高。

> ⚠️ 技术约束（已核实）：Spring Boot 3.2.2 对应 `spring-data-elasticsearch 5.2.2`，该版本**没有** `KnnQuery` 构建 API（5.3 才引入）。因此 kNN 查询需通过 `NativeQuery` 传原始 JSON 实现——项目本就是手动用 `ElasticsearchOperations` + Query DSL（已排除 ES Repository 自动配置），这条路径与现有风格一致，无需升级依赖。若团队愿意升到 Spring Boot 3.3+，则可改用类型化的 `KnnQuery`，代码更整洁。

**融合**：两路各取 Top-50，用 RRF（Reciprocal Rank Fusion）合并。RRF 的优势是**不需要归一化两路异构分数**（BM25 分值无界，余弦相似度在 [-1,1]），工程上最稳。

**同族去重**：按专利号前缀/同族标识归并，只保留同族中相关度最高的一件，避免"一屏全是同一技术"。

### 3.4 阶段③ 领域特征精排——把"专利知识"变成排序特征

这一步是**与通用方案拉开差距的地方**。语义相似度会推荐"领域相同但不可用"的专利，靠规则特征纠正：

| 特征 | 计算方式 | 作用 |
| --- | --- | --- |
| IPC 层级相似度 | 按层级前缀逐级比较（H01M4/92 vs H01M4/90 得高分；H01M vs H02K 得低分） | 技术领域一致性 |
| 法律状态 | 有效/审中加权，**失效/驳回强降权或直接过滤** | 可用性硬约束 |
| 时间衰减 | 按 `appliDate`/`publicDate` 做指数衰减 | 技术时效性 |
| 预期效果命中 | 需求 `expectedEffect` 与专利摘要/权利要求的效果表述匹配 | 直击"能不能解决我的问题" |
| 排除项冲突 | 命中 `exclusions` 则大幅降权 | 拒绝"不该推的" |
| 机构/申请人亲和 | 目标高校、合作历史 | 转化可行性 |

输出可解释的**分维度得分**（而非一个黑箱总分），为阶段④的理由生成提供事实依据——这样 AIGC 生成的理由是"基于计算结果的表述"，而不是"大模型自由发挥"。

### 3.5 阶段④ LLM 重排 + 理由生成（AIGC）

对精排后的 Top-30：

1. **交叉编码重排**：优先使用百炼的 rerank 模型（重排/`gte-rerank` 系列，具体可用型号以控制台为准）对「需求画像 × 专利文本」做交叉编码打分——交叉编码精度显著高于双塔，但无法离线预计算，只适合对少量候选做精排。
   *降级方案*：若 rerank 模型不可用或成本受限，改用 LLM 打分（给定评分标准与少量样例），效果略降但可运行。
2. **理由生成（AIGC）**：对 Top-10 生成结构化匹配理由——

```
技术特征对比：
  需求：铂基合金催化剂          → 专利：PtCo 合金催化剂        【对齐】
  需求：耐久性 5000h            → 专利：实施例记载 3000h 衰减 8%  【部分对齐，存在差距】
  需求：不涉及膜电极结构变更     → 专利：未涉及                  【无冲突】
综合匹配度：0.82    主要风险：寿命指标未达需求，需二次开发
```

**关键**：输出必须包含**差异点与风险**，而不是只讲优点。这是可解释性的真实性所在，也是答辩时的加分项——通用大模型对话很容易一味附和，而系统应能说"这件专利只有部分相关，差距在这里"。

### 3.6 阶段⑤ 校核 Agent——针对性解决幻觉

针对现状的 `notFoundPublicNums` 问题，设一个独立校核环节，对每条推荐做**可验证的校验**：

| 校核项 | 校验方式 | 不通过时 |
| --- | --- | --- |
| 专利是否存在 | 公开号在 ES 中精确命中 | 剔除并计入幻觉率指标 |
| 法律状态 | 复核当前状态是否满足可用性要求 | 降权或标注警告 |
| 理由是否有据 | 理由中引用的技术特征必须在专利原文中可检索到 | 标记"理由存疑"，要求重新生成 |
| 分类号是否属实 | LLM 声称的 IPC 是否与库中一致 | 以库中值为准覆盖 |

**设计原则：让幻觉无处藏身——凡是可验证的陈述都必须回到数据源核对。** 这条原则本身就是本方案的核心创新点，也是"垂直场景"相对"通用对话"的价值证明。

### 3.7 Agent 编排

三个 Agent 协作（体现赛道的 Intelligent Agent 要求）：

- **需求澄清 Agent**：需求描述含糊时主动多轮追问（"工况条件？指标量级？是否已有技术路线？"），补全技术画像。可复用现有 `ChatService` / WebSocket 流式通道（`/ws/ai-chat` 已具备流式能力）。
- **检索策略 Agent**：查询改写与扩展（同义词、上下位词、专业术语归一）、按 IPC 分层递进检索、召回不足时自动放宽条件并重新召回。对应现状"一条查询查到底"的不足。
- **校核 Agent**：见 3.6，作为流水线的守门人。

编排上建议**轻量自研**（基于现有 Service 层 + 明确的阶段状态机），而不是引入重型 Agent 框架——既复用现有代码，也便于在答辩中讲清每一步的可控性（评审通常不喜欢"黑箱 Agent"）。

---

## 4. 功能模块清单

> 对应竞赛要求的「功能模块」说明。

### 4.1 后端模块

| 模块 | 职责 | 复用现有 |
| --- | --- | --- |
| `RequirementProfileService` | 需求结构化抽取（AIGC）+ 人工修正 | `DashScopeChatService` |
| `EmbeddingService` | 专利/需求文本向量化、批量与限流控制 | 参考 `ai/oss_qa` 的批量与退避策略 |
| `HybridRecallService` | BM25 + kNN 双路召回、RRF 融合、同族去重 | `PatentEsService.buildQuery` |
| `PatentRankingService` | 领域特征精排、分维度得分 | 新增（纯规则，易测） |
| `MatchExplainService` | rerank + 理由生成（AIGC） | `DashScopeChatService` |
| `MatchAuditAgent` | 幻觉校核与理由溯源 | 新增 |
| `MatchingEvalService` | 离线评测：指标计算、基线对比、消融 | 新增 |
| `MatchFeedbackService` | 采纳/驳回/修正反馈入库，闭环迭代 | 新增 |

### 4.2 数据模型（增量）

```sql
-- 需求技术画像
requirement_profile(
  requirement_id PK, profile_json JSON, source ENUM('AI','HUMAN','EDITED'),
  model, confidence, created_at, updated_at
)

-- 匹配结果扩展（现有 requirement_patent_match 增列）
ALTER TABLE requirement_patent_match
  ADD COLUMN score_detail_json JSON,      -- 分维度得分
  ADD COLUMN evidence_json JSON,          -- 技术特征对齐/差异证据
  ADD COLUMN rank_no INT,
  ADD COLUMN audit_status VARCHAR(16);    -- PASSED / WARNED / REJECTED

-- 评测标注集
match_eval_set(id, requirement_id, patent_public_num, relevance TINYINT,  -- 2强/1弱/0不相关
               labeled_by, labeled_at, note)

-- 人工反馈
match_feedback(id, requirement_id, patent_public_num, action ENUM('ACCEPT','REJECT','CORRECT'),
               reason, user_id, created_at)
```

ES 侧：`patents` 索引 mapping 增加 `embedding`（`dense_vector`, `dims` 与所选 embedding 模型一致，`index: true` 用于 kNN）。

### 4.3 接口设计

| 接口 | 说明 |
| --- | --- |
| `POST /api/requirements/{id}/profile` | 生成/更新需求技术画像（支持人工修正回写） |
| `GET /api/requirements/{id}/match-patents/v2` | 返回**带分数、排序、分维度得分、匹配理由、校核状态**的结果 |
| `POST /api/requirements/{id}/match-feedback` | 采纳/驳回/修正反馈 |
| `GET /api/admin/matching/eval` | 触发评测并返回指标（含与基线对比、消融结果） |
| `POST /api/requirements/clarify`（流式） | 需求澄清多轮对话（复用 WebSocket） |

> 保留 `v1` 旧接口作为基线对照，评测时两者可并行调用——这直接构成"改进前 vs 改进后"的演示材料。

### 4.4 前端交互升级（`RequirementManage.vue`）

现状弹窗：**无排序、无评分、无分页、无逐条理由**，只有类别/公开号/标题/申请人/发明人五列，且「保存匹配结果」把全部结果一次性落库、无选择环节。

升级后：
1. **匹配结果按综合分降序排列**，新增「匹配度」列（分数 + 可视化进度条）与「校核状态」标签；
2. 行展开显示**分维度得分雷达/条形**与**技术特征对比表**（对齐/差异/风险）；
3. 增加**复选框**，人工勾选后才持久化（对齐竞赛与业务语义上的"人工确认"）；
4. 每条提供**采纳/驳回**按钮，写入反馈表；
5. 展示需求画像，允许人工修正后重新匹配（体现 AIGC + Human-in-the-loop）。

顺带修复已发现的缺陷（低风险、提升演示观感）：
- 专家匹配字段名不匹配：后端返回 `{userId, field, expertise, contactInfo}`，前端读 `{id, name}` → 界面出现「专家 #undefined」，落库 `expertUserId` 为 `null`；
- 持久化时 `patentSource` 被硬编码为 `EXTERNAL`，来源为 `USER` 的专利被写错；
- 弹窗标题等处存在源码编码损坏的乱码文案。

---

## 5. 精确度评测体系

> 这是本方案的**核心竞争力**，也是竞赛答辩最容易被追问的点：你怎么知道变准了？

### 5.1 标注集构建

- 规模：建议 50~100 条真实需求 × 每需求人工标注 20~30 件候选专利（混合强相关/弱相关/不相关）。
- 相关性等级：2 = 强相关（技术手段可解决该需求）、1 = 弱相关（同领域但手段不同）、0 = 不相关。
- 来源：平台真实需求 + 平台专利库；标注由团队成员交叉双标，分歧讨论后定稿（可报告标注一致性，进一步加分）。
- **重要**：标注集必须固定版本并冻结，否则指标不可比。

### 5.2 对比实验设计

| 组别 | 配置 | 目的 |
| --- | --- | --- |
| 基线（现状） | 大模型直答报号 + ES 兜底，即当前 `AiController.chatWithPatent` | 证明起点 |
| +需求结构化 | 仅加阶段① | 消融：结构化的贡献 |
| +混合召回 | 加阶段②（稠密腿） | 消融：语义召回的贡献 |
| +领域精排 | 加阶段③ | 消融：领域规则的贡献 |
| +LLM 重排 | 加阶段④ | 消融：交叉编码的贡献 |
| 完整方案 | 全部 + 阶段⑤ | 最终指标 |

**消融实验是本方案最有说服力的部分**：它把"精确度提升"拆解成可归因的贡献，而不是笼统归功于"用了大模型"。

### 5.3 实现方式

评测脚本建议用 Python 实现（可复用仓库中 `ai/oss_qa/` 的既有基建：文档抽取、分块、向量化、批量与限流处理），通过 HTTP 调用后端接口取结果，本地计算指标并输出对比表。产出物：
- 指标对比表（Markdown/CSV）
- 错误分析：按失败类型归类（漏召 / 排序错 / 领域错配 / 幻觉 / 排除项违反）
- 可用于答辩的图表

**错误分析比总分更重要**——能说清"我们错在哪、为什么错、下一版怎么修"，是成熟度最直接的体现。

---

## 6. 工具与平台

| 层次 | 选型 | 说明 |
| --- | --- | --- |
| 后端 | Spring Boot 3.2.2 / Java 17 | 现有系统 |
| 检索 | Elasticsearch 8.12.2 | 现有；BM25 复用 + 新增 `dense_vector` kNN |
| 数据库 | MySQL 8 | 现有；画像/评测/反馈表增量扩展 |
| 大模型（生成） | 阿里云百炼 DashScope，`deepseek-v3.2` | 现有 `DashScopeChatService`（OpenAI 兼容模式） |
| 向量化 | 百炼 embedding 系列 | 仓库 `ai/index/manifest.json` 显示已用 `text-embedding-v2` 成功建过索引，证明该账号可用 |
| 重排 | 百炼 rerank 系列（具体型号以控制台为准） | 不可用时降级为 LLM 打分 |
| 实时通信 | Spring WebSocket（原生 Handler） | 现有 `/ws/ai-chat`，用于澄清对话流式输出 |
| 前端 | Vue 3 + TypeScript + Element Plus + ECharts | 现有；分数可视化用 ECharts |
| 端侧 | Capacitor（Android） | 现有，可作演示加分项 |
| 评测 | Python 3 + 现有 `ai/oss_qa` 基建 | 指标计算与错误分析 |
| 辅助编码 | ZCode / 各类 AI 编程助手 | 开发效率工具（如实说明即可） |

---

## 7. 实施计划

按"每阶段都有可演示产出"的原则切分，便于中途汇报与风险控制。

| 阶段 | 内容 | 产出 | 建议工时 |
| --- | --- | --- | --- |
| P0 | 后端可构建可运行（**前置阻塞项，见风险**）+ 修 4.4 的三处缺陷 | 能跑起来的系统 | 2~3 天 |
| P1 | 需求结构化 + 画像表 + 前端画像展示 | 可演示：需求 → 结构化画像 | 4~5 天 |
| P2 | 向量化 + ES mapping 扩展 + 重建索引 + kNN 召回 + RRF 融合 | 可演示：语义召回命中关键词召回不到的结果 | 6~8 天 |
| P3 | 领域特征精排（IPC 层级、法律状态、时间衰减、排除项） | 可演示：分维度得分 | 4~5 天 |
| P4 | LLM 重排 + 理由生成 | 可演示：技术特征对比表 | 4~5 天 |
| P5 | 校核 Agent + 幻觉率指标 | 可演示：幻觉被拦截 | 3~4 天 |
| P6 | 评测集标注 + 评测脚本 + 消融实验 | **核心交付物**：指标对比与错误分析 | 5~7 天（标注可并行） |
| P7 | 前端排序/勾选/反馈、Agent 澄清对话 | 完整交互 | 5~6 天 |
| P8 | 演示脚本、案例准备、答辩材料 | 演示视频与文档 | 3~4 天 |

**关键路径提示**：P6 的标注集应尽早启动（可与 P2 并行），因为它是所有精确度结论的地基；若等到最后才标注，容易发现指标不如预期而来不及调优。

---

## 8. 演示方案与案例

竞赛要求"必须提供可运行的完整软件系统和可在计算机上演示的效果"且"要有案例"。建议准备三个案例：

### 案例 A：正例——精确召回（氢能）
需求：「提高氢燃料电池催化剂在启停工况下的耐久性，目标寿命 5000 小时以上」
演示：需求画像抽取 → 混合召回（对比展示"只做关键词召回会漏掉哪些"）→ 精排 → 匹配理由（含差异点：某专利寿命仅 3000h）→ 人工确认 → 反馈入库。

### 案例 B：负例——诚实拒答（体现系统不谄媚）
需求：构造一条库内确实没有合适专利的需求（如某冷门交叉领域）。
演示：系统输出"未找到高匹配度专利"，并给出**为什么不匹配**的分析与建议（放宽哪些条件可能有结果）。这一点极能体现系统的**校核与自省能力**，比一味给结果更有说服力。

### 案例 C：Agent 闭环——澄清与迭代
需求：故意给一条含糊需求（「想改进电池性能」）。
演示：澄清 Agent 多轮追问补全 → 画像成型 → 匹配 → 用户驳回一条 → 系统记录反馈并说明影响。

### 演示中必须插入的对比画面
同一需求下，**基线结果 vs 完整方案结果**并排展示（基线可用旧接口 `v1` 现场调用），让评委直接看到：
- 基线推荐里的幻觉专利号（`notFoundPublicNums` 非空）
- 完整方案的同一条推荐带有分维度得分与技术特征对比
- 指标表：Precision@5、nDCG@10、幻觉率的前后变化

---

## 9. 创新点提炼（答辩用）

1. **面向专利文本的领域结构化匹配**——不止做语义相似度，而是把 IPC 层级、法律状态、时间属性、排除项作为一等排序特征，解决"语义相似但业务不可用"的通用方案通病。
2. **可校核的匹配（幻觉结构化拦截）**——凡可验证的陈述一律回到数据源核对；专利号、法律状态、IPC、理由引用逐条验证，把"防幻觉"从提示词祈祷变成工程约束。
3. **可解释且包含差异与风险**——AIGC 生成的不是营销文案，而是基于精排计算结果的**技术特征对比表**，并明确给出差距与风险。
4. **评测驱动的精确度提升**——自建标注集 + 基线 + 消融实验，把"精确度"从主观感受变成可复现指标，并完成错误归因。
5. **Human-in-the-loop 与反馈闭环**——人工确认与修正既是业务要求，也持续产出训练/评测数据。

---

## 10. 风险与取舍（务必在答辩前想清）

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| **本机无法构建后端**（已实测） | **阻塞全部开发** | 机器仅有 JDK 25，项目要求 Java 17；Lombok 1.18.32 在 JDK 25 下报 `ExceptionInInitializerError: com.sun.tools.javac.code.TypeTag :: UNKNOWN`（已用未修改代码复现，确认与业务代码无关）。**必须先装 JDK 17** |
| 向量化成本与限流 | 大批量重建索引可能触发 429 | 批量 + 退避 + 断点续传（仓库 `ai/oss_qa` 已有 `--batch-size`/`--sleep` 经验可借鉴）；增量索引仅对新专利做 |
| 标注集工作量 | 占用大量人力，易拖期 | 先做小规模（20 需求）验证流程，再扩到目标规模；可用 LLM 预标注 + 人工复核加速，但**必须人工复核**，否则指标不可信 |
| rerank 模型可用性/成本 | 阶段④降级 | 预备 LLM 打分降级方案，效果略降但不影响整体架构成立 |
| ES mapping 变更需重建 | 演示环境重建耗时 | 复用现有 `reindexDataset`/`reindexAll`；提前在演示环境预建好索引 |
| 指标提升不达预期 | 答辩被动 | 提前跑通基线拿到真实数字；**口径诚实**，重点讲清归因与改进方向，而非夸大数字 |
| Agent 复杂度失控 | 工期风险 | 轻量自研编排，控制 Agent 数量为 3 个，每个职责单一、可单独测试 |

### 建议的取舍

- **不建议**引入新的向量数据库（Milvus/pgvector 等）：ES 已能满足，新增组件对竞赛项目只增加部署与演示风险，收益有限。
- **不建议**做大而全的对话式 Agent 平台：本选题胜在**垂直深度与可验证性**，而非 Agent 数量。
- **不建议**在方案阶段臆定提升目标数字：先测基线，用真实数据说话。

---

## 附录 A：与现状的代码对应关系

| 环节 | 现状实现 | 本方案 |
| --- | --- | --- |
| 入口 | `RequirementManage.vue` L461-501 → `POST /api/ai/chat/patent` | 新增 `match-patents/v2`，旧接口保留作基线 |
| 检索 | **无检索**，提示词要求大模型报专利号（`AiController` L136-149） | 需求结构化 + BM25/kNN 混合召回 |
| 专利号来源 | `extractPublicNums` 正则从生成文本抠号（L150） | 检索结果直接来自索引，无抠号环节 |
| 幻觉 | `notFoundPublicNums` 仅记录 | 校核 Agent 拦截 + 幻觉率指标 |
| 评分 | DTO 无 score 字段；`matchScore` 恒 NULL | 分维度得分 + 综合分 + 排序 |
| 排序 | 无（拼装顺序） | RRF 融合 + 领域精排 + LLM 重排 |
| 理由 | 无（仅有整段 `aiAnalysis`） | 逐条技术特征对比表（AIGC） |
| 领域特征 | 未使用 | IPC 层级 / 法律状态 / 时间衰减 / 排除项 |
| 持久化 | 全量落库、`patentSource` 硬编码 `EXTERNAL` | 人工勾选后落库，来源正确，附分数与理由 |
| 精确度度量 | **无** | 标注集 + Precision@K / nDCG / MRR / 幻觉率 + 消融 |

---

*本方案的"现状实测"部分逐行核对自当前代码（`AiController`、`PatentEsService`、`RequirementController`、`RequirementManage.vue`、`pom.xml`、`docs/elasticsearch/docker-compose-no-ik.yml`），非推测。*
