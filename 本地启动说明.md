# 本地启动说明（本机适配版）

> 生成日期：2026-09-21
> 适用目录：`C:\Users\qiye0\Desktop\知识产权平台`
> 本机环境实测结论，非文档推理。

---

## 1. 一句话结论

**前端现在就能打开（已实测渲染正常），后端只差 MySQL 口令这一个信息。**

- 前端地址：<http://localhost:5173>
- 后端地址：<http://127.0.0.1:8081>（**不是 8080**，原因见 §4）

---

## 2. 前端：开箱即用

```powershell
cd C:\Users\qiye0\Desktop\知识产权平台\Patent-Management-Platform-修改1
$env:VITE_API_PROXY_TARGET="http://127.0.0.1:8081"
$env:VITE_WS_PROXY_TARGET="ws://127.0.0.1:8081"
npm run dev
```

Git Bash 写法：

```bash
cd "C:/Users/qiye0/Desktop/知识产权平台/Patent-Management-Platform-修改1"
VITE_API_PROXY_TARGET=http://127.0.0.1:8081 \
VITE_WS_PROXY_TARGET=ws://127.0.0.1:8081 npm run dev
```

- 依赖已装好（`node_modules` 存在），无需 `npm install`。
- 必须带这两个环境变量：`vite.config.ts` 的默认代理目标是 8080，而 8080 在本机被别的软件占用了。
- 浏览器访问 <http://localhost:5173>。注意用 `localhost`，Vite 只监听 IPv6 回环，用 `127.0.0.1:5173` 打不开。

不带后端时页面骨架、导航、转化案例都能正常显示，但数据区会弹出「服务器内部错误，请稍后重试」。

---

## 3. 后端：需要先补 MySQL

### 3.1 唯一的阻塞点

后端启动实测失败，日志明确：

```
Caused by: java.sql.SQLException: Access denied for user 'root'@'localhost' (using password: YES)
	at com.mysql.cj.jdbc.ConnectionImpl.createNewIO(ConnectionImpl.java:815)
```

原因：`patent/patent/config/application-local.properties` 里写的是 `DB_PASSWORD=root`，但本机 MySQL 是 **2026-09-14 新装的**，root 口令不是它。已试过 `root`、`123456`、空口令、`password`、`admin`、`mysql` 均被拒。

**你需要做的一件事**：把安装 MySQL 时设的 root 口令填到

```
C:\Users\qiye0\Desktop\知识产权平台\patent\patent\config\application-local.properties
```

的 `DB_PASSWORD=` 后面。

### 3.2 数据库还不存在

本机 MySQL 数据目录（`D:\MySQL\data`）里只有 `db01`、`mysql`、`performance_schema`、`sys`，**没有 `patent` 库**。也就是说这个平台在这台机器上从未建过库。

建库 + 导表（口令填你的真实口令）：

```bash
cd "C:/Users/qiye0/Desktop/知识产权平台/patent/patent/docs"

"/d/MySQL/bin/mysql" -uroot -p -e "CREATE DATABASE IF NOT EXISTS patent DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;"

"/d/MySQL/bin/mysql" -uroot -p patent < database_schema.sql
"/d/MySQL/bin/mysql" -uroot -p patent < cms_schema.sql
```

`database_schema.sql` 已包含 28 张表（5 张专利分表 + 用户/组织/专家/需求/匹配/评估/转化/通知/同步/审计/AI 会话），`cms_schema.sql` 补 `home_content` 与 `cms_article`。两个脚本都是 `CREATE TABLE IF NOT EXISTS`，可重复执行。

**可选补丁**（只有要用到「审计日志」和「评估数据集」功能才需要）：

```sql
-- 审计日志：database_schema.sql 里的 audit_log 缺这些列
ALTER TABLE audit_log
  ADD COLUMN username VARCHAR(64) NULL,
  ADD COLUMN display_name VARCHAR(128) NULL,
  ADD COLUMN role_name VARCHAR(64) NULL,
  ADD COLUMN event_type VARCHAR(32) NULL,
  ADD COLUMN request_method VARCHAR(16) NULL,
  ADD COLUMN request_path VARCHAR(255) NULL,
  ADD COLUMN ip_address VARCHAR(64) NULL,
  ADD COLUMN mac_address VARCHAR(64) NULL,
  ADD COLUMN host_name VARCHAR(128) NULL,
  ADD COLUMN location VARCHAR(128) NULL,
  ADD COLUMN user_agent VARCHAR(512) NULL,
  ADD COLUMN platform_type VARCHAR(32) NULL,
  ADD COLUMN operation_result VARCHAR(16) NULL;

-- 评估报告：关联数据集来源
ALTER TABLE patent_valuation_report
  ADD COLUMN dataset_id BIGINT NULL,
  ADD COLUMN record_id VARCHAR(128) NULL;
```

> 仓库里 `src/main/resources/audit_log_upgrade.sql` 用的是 `ADD COLUMN IF NOT EXISTS`，这是 MariaDB 语法，**MySQL 8 会直接报错**，所以上面给的是改写版。

### 3.3 启动后端

本机没装 Maven，但 `target/` 里已有可用的 fat jar（71 MB，源码无更新），直接用 jar 启动：

```powershell
cd C:\Users\qiye0\Desktop\知识产权平台\patent\patent
java -jar target\patent-0.0.1-SNAPSHOT.jar --server.port=8081
```

已验证可行的部分：**Java 25 运行环境没问题**（原 pom 目标是 Java 17，实测能正常加载 Spring 上下文），**ES 关掉也不影响启动**。

---

## 4. 两个本机专属的坑

| 坑 | 现象 | 处理 |
| --- | --- | --- |
| **8080 被占用** | 请求全返回 `Access Error: 404 -- Not Found`，响应头 `Server: Embedthis-http` | 占用者是 `ApplicationWebServer.exe`（PID 12884），**不属于本项目，别杀**。后端改用 8081，前端用 `VITE_API_PROXY_TARGET` 指过去 |
| **Elasticsearch 未安装** | 9200 无监听，全盘找不到 elasticsearch 目录 | 已在 `config/application-local.properties` 把 `ES_ENABLED` 改为 `false`。**代价：专利全文检索（`/api/patents/es/**`）不可用**，只能走 MySQL 分表直查兜底（`/api/patents?category=`）。要用检索得另装 ES 并把该值改回 `true` |

---

## 5. 备注

- **平台没有任何数据。** 所有 SQL 脚本都只建表，不含 `INSERT`（实测 `grep -c "INSERT INTO" database_schema.sql` 为 0）。桌面和项目内也没有数据库备份。5 张专利分表、需求、专家、评估记录全是空的，检索页会查出空结果。
- **AI 助手不可用**：`DASHSCOPE_API_KEY` 那个 Key 已在 2026-09-21 被确认泄漏，需要在阿里云百炼控制台重签后填回 `config/application-local.properties`（详见桌面 `Patent_Platform\密钥处置说明.md`）。
- **后台管理登录**：仓库内配置已不提供默认口令，未配置 `ADMIN_AUTH_PASSWORD` 时后台登录直接返回 503。本机真实口令写在 `patent/patent/config/application-local.properties`（该文件不入库），需要时自行查看。
- **重复副本**：桌面另有一份 `Patent_Platform\`（2026-09-21 生成的脱敏版，含随机 JWT 密钥、已有 git 仓库但尚无提交）。两份都能跑，要求相同。
- 后端进程与前端 dev server 是分开的，关掉终端窗口即停；重新打开按 §2 / §3.3 再跑一次即可。
